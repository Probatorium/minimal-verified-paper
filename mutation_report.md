# Mutation study

Generated by `mutate.py`. Every row is one deliberate corruption of a
throwaway copy of this package, followed by a full run of `verify.py`
inside that copy.

**killed** counts assertions that ran and failed. **not reached** counts
assertions that never ran because a gate check stopped the suite first;
those are caught even harder, but counting them as killed would flatter
the suite, so they are kept apart.

```
MUTANTS: 17
MIN_CHECKS_KILLED: 1
MAX_CHECKS_KILLED: 35
```

The baseline run of this package was clean: every assertion passed
before any mutation was applied.

## Summary

| Mutant | Family | Killed | Not reached |
| ------ | ------ | -----: | ----------: |
| `data_digit_flipped` | input data | 10 | 0 |
| `data_tail_zeroed` | input data | 35 | 0 |
| `data_truncated` | input data | 1 | 95 |
| `data_adjacent_swap` | input data | 1 | 0 |
| `cited_constant_corrupted` | input data | 2 | 0 |
| `manuscript_number_edited` | published surface | 2 | 0 |
| `frozen_value_edited` | published surface | 2 | 0 |
| `result_hardcoded_in_source` | published surface | 1 | 0 |
| `figure_gone_stale` | published surface | 2 | 0 |
| `forbidden_phrase_reintroduced` | published surface | 1 | 0 |
| `front_matter_drifted` | published surface | 1 | 0 |
| `front_matter_title_drifted` | published surface | 1 | 0 |
| `front_matter_title_yaml_only` | published surface | 1 | 0 |
| `front_matter_author_byline_only` | published surface | 1 | 0 |
| `front_matter_year_citation_field` | published surface | 1 | 0 |
| `manuscript_duplicate_number_edited` | published surface | 1 | 0 |
| `manuscript_table_row_edited` | published surface | 1 | 0 |

## What each mutant killed

### `data_digit_flipped`

Family: input data. Target: `src/data_path_a_spigot.py`.

Expected before running: One digit of path A is wrong. The two paths should disagree, two counts should move, and every downstream statistic with them.

Killed 10 assertion(s): `DUAL-chi_square`, `DUAL-count_0`, `DUAL-count_9`, `DUAL-digits`, `DUAL-p_value`, `FIG-matches-data`, `FRZ-chi_square`, `FRZ-count_0`, `FRZ-count_9`, `FRZ-p_value`

### `data_tail_zeroed`

Family: input data. Target: `src/data_path_a_spigot.py`.

Expected before running: A fifth of the sequence is replaced by zeros. Structurally still a digit sequence, so it passes the gate and is caught by the statistics instead. Should be the worst-hit mutant.

Killed 35 assertion(s): `DUAL-chi_square`, `DUAL-count_0`, `DUAL-count_1`, `DUAL-count_2`, `DUAL-count_3`, `DUAL-count_4`, `DUAL-count_5`, `DUAL-count_6`, `DUAL-count_7`, `DUAL-count_8`, `DUAL-count_9`, `DUAL-digits`, `DUAL-largest_count`, `DUAL-p_value`, `DUAL-post_hoc_p`, `DUAL-post_hoc_p_adjusted`, `DUAL-post_hoc_z`, `FIG-matches-data`, `FRZ-chi_square`, `FRZ-count_0`, `FRZ-count_1`, `FRZ-count_2`, `FRZ-count_3`, `FRZ-count_4`, `FRZ-count_5`, `FRZ-count_6`, `FRZ-count_7`, `FRZ-count_8`, `FRZ-count_9`, `FRZ-largest_count`, `FRZ-p_value`, `FRZ-post_hoc_p`, `FRZ-post_hoc_p_adjusted`, `FRZ-post_hoc_z`, `SPLIT-no-hardcoded-results`

### `data_truncated`

Family: input data. Target: `src/data_path_b_machin.py`.

Expected before running: One digit short. Should die at the gate, before any statistic is computed, and therefore kill few assertions while blocking all of them.

The suite stopped at the structural gate. The statistics were never
computed, which is the intended behaviour for input this broken.

Killed 1 assertion(s): `INV-len-b`

Never reached 95 assertion(s), the first being `CIT-agrees-a`.

### `data_adjacent_swap`

Family: input data. Target: `src/data_path_a_spigot.py`.

Expected before running: Two digits swapped deep in the sequence. Counts are unchanged, so every statistic is unchanged. Only the comparison between the two paths can notice. This is the mutant that shows what the suite does NOT cover.

Killed 1 assertion(s): `DUAL-digits`

### `cited_constant_corrupted`

Family: input data. Target: `src/cited_constants.py`.

Expected before running: The published anchor is altered. Both computation paths should part company with it.

Killed 2 assertion(s): `CIT-agrees-a`, `CIT-agrees-b`

### `manuscript_number_edited`

Family: published surface. Target: `paper.md`.

Expected before running: The prose is edited and nothing else. The headline mechanism: the frozen figure is no longer in the text, and the new number is covered by no claim.

Killed 2 assertion(s): `COV-orphan-numbers`, `FRZ-chi_square`

### `frozen_value_edited`

Family: published surface. Target: `src/frozen_claims.py`.

Expected before running: The frozen value is edited and the prose is not. The same mechanism from the other side.

Killed 2 assertion(s): `COV-orphan-numbers`, `FRZ-p_value`

### `result_hardcoded_in_source`

Family: published surface. Target: `src/stats_path_a_series.py`.

Expected before running: A computed result is written into the code by hand, where it can drift from the computation. The cited/computed split should catch it.

Killed 1 assertion(s): `SPLIT-no-hardcoded-results`

### `figure_gone_stale`

Family: published surface. Target: `figures/digit_frequencies.svg`.

Expected before running: The committed figure no longer shows the committed data.

Killed 2 assertion(s): `FIG-labels-counts`, `FIG-matches-data`

### `forbidden_phrase_reintroduced`

Family: published surface. Target: `paper.md`.

Expected before running: A sentence this paper is not entitled to write is added back.

Killed 1 assertion(s): `PHR-paper.md`

### `front_matter_drifted`

Family: published surface. Target: `README.md`.

Expected before running: One accent dropped on one surface. Exact comparison should catch what a fuzzy one would not.

Killed 1 assertion(s): `FM-author-README.md-byline`

### `front_matter_title_drifted`

Family: published surface. Target: `CITATION.bib`.

Expected before running: The subtitle is dropped from the BibTeX record and from nowhere else. The title is the longest string this package compares and the one most likely to drift, and CITATION.bib is the surface a citation is built from. Should die on that surface and on no other, showing that the front matter check bites on the title and not only on the author.

Killed 1 assertion(s): `FM-title-CITATION.bib-field`

### `front_matter_title_yaml_only`

Family: published surface. Target: `paper.md`.

Expected before running: The title drifts in the YAML block while the level-one heading keeps the full string. This escaped before the front matter check started reading declared locations, because a whole-file search found the title in the heading and was satisfied.

Killed 1 assertion(s): `FM-title-paper.md-yaml`

### `front_matter_author_byline_only`

Family: published surface. Target: `paper.md`.

Expected before running: The accent drops from the byline while the YAML field keeps it. Escaped for the same reason as the title above: two copies, one search.

Killed 1 assertion(s): `FM-author-paper.md-byline`

### `front_matter_year_citation_field`

Family: published surface. Target: `CITATION.bib`.

Expected before running: The year field changes while the citation key keeps the digits 2026 inside it. Escaped because a substring search could not tell a correct year from a stale one sitting beside a key that happens to contain the same digits. The check now reads the field.

Killed 1 assertion(s): `FM-year-CITATION.bib-field`

### `manuscript_duplicate_number_edited`

Family: published surface. Target: `paper.md`.

Expected before running: The expected count is stated twice in its section, and one copy is edited. Escaped because presence in the section was satisfied by the untouched copy. The claim now freezes how many times the number must appear.

Killed 1 assertion(s): `FRZ-expected_count`

### `manuscript_table_row_edited`

Family: published surface. Target: `paper.md`.

Expected before running: One cell of the results table is edited. Two digits share the count 93 and the replacement 94 is another digit's count, so neither presence nor the orphan-number scan could see it. Each row is now anchored to its own line.

Killed 1 assertion(s): `FRZ-count_0`

